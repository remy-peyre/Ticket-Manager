{% extends 'TicketBundle:Default:layout.html.twig' %}

{% block main %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button>Change ticket</button>
    {{ form_end(form) }}
{% endblock %}
